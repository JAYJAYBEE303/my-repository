# Claude Code working files
.claude/

# Impeccable plugin working files (config + hook cache), same class as .claude/
.impeccable/

# Jekyll build output (GitHub Pages rebuilds on push; local builds stay local)
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/

# OS junk
Thumbs.db
.DS_Store
desktop.ini
~$*
*.tmp

# Project notes not for the public site
SKILL.md
Portrait 1.png
Portrait 2.png
Portrait 3.png

# Work documents — never part of a public portfolio site
*.docx
*.xlsx
*.xls
*.csv

# All PDFs blocked except the one public file
*.pdf
!files/CV.pdf

# Bundler / editor
.bundle/
.vscode/

# Large binaries — GitHub rejects files over 100 MB
*.potx
*.pptx
*.psd
*.mp4
*.mov
*.zip

# AMF1 work content — must never end up public
*AMF1*
*Aston*
